services:
  base:
    user: root
    working_dir: /opt/streamient
    restart: unless-stopped
    image: streamient-dev:local
    volumes:
      - ./:/opt/streamient
      - node_modules:/opt/streamient/node_modules
      - pnpm_store:/pnpm/store
